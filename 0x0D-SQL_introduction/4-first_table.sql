-- prints the full description of the table first_table from the database hbtn_0c_0
-- show tables from commands that display tables of a databases
CREATE TABLE IF NOT EXISTS first_table (id INT,
name VARCHAR(256));